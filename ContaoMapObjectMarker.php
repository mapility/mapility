<?php if (!defined('TL_ROOT')) die('You can not access this file directly!');

/**
 * PHP version 5
 * @copyright  Cyberspectrum 2012
 * @author     Christian Schiffler <c.schiffler@cyberspectrum.de>
 * @package    ContaoMaps
 * @license    LGPL 
 * @filesource
 */


/**
 * class ContaoMapObject
 */
class ContaoMapObjectMarker extends ContaoMapObject
{
/*
			$tmp['id'] = $objMarkers->id;
			$tmp['longitude'] = $objMarkers->longitude;
			$tmp['latitude'] = $objMarkers->latitude;
			$tmp['altitude'] = $objMarkers->altitude;
			if($objLayer->zindex)
				$tmp['zindex'] = $objLayer->zindex;
			$objMarkers->icon=deserialize($objMarkers->icon);
			if($objMarkers->name)
				$tmp['name'] = $objMarkers->name;
			if($objMarkers->icon)
			{
				$tmp['icon'] = $objMarkers->icon;
				$objIcon = new File($objMarkers->icon);
				$tmp['iconsize'] = $objIcon->width.','.$objIcon->height;
				if($objMarkers->anchor)
					$tmp['iconposition'] = $objMarkers->anchor;
				else
					$tmp['iconposition'] = sprintf('%s,%s', floor($objIcon->width/2), floor($objIcon->height/2));
			}
			if($objMarkers->shadow)
				$tmp['shadow'] = $objMarkers->shadow;
			if($objMarkers->shadowsize)
				$tmp['shadowsize'] = $objMarkers->shadowsize;
			if($objMarkers->text)
				$tmp['infotext'] = $objMarkers->text;
			if($objMarkers->info_anchor)
				$tmp['infoposition'] = $objMarkers->info_anchor;
			if($objMarkers->info_auto)
				$tmp['autoinfo'] = $objMarkers->info_auto;
*/
}

?>